declare module "*.png"


// .d.ts files are global type declaration files.
// TypeScript loads them automatically as long as they are inside a folder
// included in tsconfig.json (e.g., "include": ["src"]).
// This is why we don't import them manually.